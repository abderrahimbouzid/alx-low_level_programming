#include "main.h"
/**
 * swap_int - check the code
 * @a: the integer to check
 * @b: the integer to check
 *
 * Return: Always void.
 */
void swap_int(int *a, int *b)
{
int c;
c = *a;
*a = *b;
*b = c;
}
